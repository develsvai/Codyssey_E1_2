# 결정

- 시험 대비 문서는 기존 `doc/learning-objectives-rationale.md`를 덮어쓰지 않고 새 파일 `doc/python-exam-summary.md`로 분리했다.
- 문서 구성은 시험 직전 복습 흐름에 맞춰 우선순위, 개념 설명, 답변 템플릿, 예상 질문, 체크리스트 순서로 잡았다.
- 미션 원문과 기존 구현에서 반복되는 주제인 클래스/객체, 기본 문법, JSON 파일 입출력, 예외 처리, 실행 흐름, Git 기초를 중심으로 정리했다.
- README에는 별도 섹션을 늘리지 않고 기존 "설계 문서" 문단과 파일 구조에 링크를 추가해 접근성을 보강했다.
- Active Workflow Memory의 core/cli 분리 원칙을 반영해 `core`, `cli`, `main.py`의 책임을 시험 답변 문장으로 정리했다.
