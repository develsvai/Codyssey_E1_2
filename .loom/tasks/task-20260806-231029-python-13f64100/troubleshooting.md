# 트러블슈팅

특별한 실패나 차단 사항은 없었다.

확인한 사항:

- README에서 새 문서 링크가 검색되는지 확인했다.
- `doc/python-exam-summary.md` 파일이 실제로 생성되었는지 확인했다.
- `git diff --check`로 공백 오류가 없는지 확인했다.
- `loom validate --strict`로 Loom metadata 일관성을 확인했다.
